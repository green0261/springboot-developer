spring:
    jpa:
      #전송 쿼리 확인
      show-sql : true
      properties :
        hibernate :
          format_sql : true
      #애플리케이션을 실행할 때 테이블 생성 후 data.sql 파일에 있는 쿼리를 생성하도록 하는 옵션
      defer-datasource-initialization: true
    datasource:
      url: jdbc:h2:mem:testes
      username: sa
    h2:
      console:
        enabled: true
jwt:
  issuer: ajufresh@gmail.com
  secret_key: study-springboot